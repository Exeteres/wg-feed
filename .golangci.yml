version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - misspell
    - ineffassign
    - unconvert
    - bodyclose
    - nilerr
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
